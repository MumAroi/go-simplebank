info:
  name: list account
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:8081/accounts?page_id=1&page_size=10
  params:
    - name: page_id
      value: "1"
      type: query
    - name: page_size
      value: "10"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
