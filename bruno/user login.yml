info:
  name: user login
  type: http
  seq: 6

http:
  method: POST
  url: http://localhost:3010/v1/login_user
  body:
    type: json
    data: |
      {
        "username": "areyouwanno1",
        "password": "Pas112233"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
