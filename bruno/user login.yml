info:
  name: user login
  type: http
  seq: 6

http:
  method: POST
  url: http://localhost:3010/users/login
  body:
    type: json
    data: |
      {
        "username": "areyouwanno",
        "password": "Pas112233"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
