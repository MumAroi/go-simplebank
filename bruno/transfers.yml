info:
  name: transfers
  type: http
  seq: 4

http:
  method: POST
  url: http://localhost:8081/transfers
  body:
    type: json
    data: |
      {
        "from_account_id": 1,
        "to_account_id": 3,
        "amount": 10,
        "currency": "USD"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
