info:
  name: create user
  type: http
  seq: 5

http:
  method: POST
  url: http://localhost:3010/users
  body:
    type: json
    data: |
      {
        "username": "areyouwanno3",
        "password": "Pas112233",
        "full_name": "por mas",
        "email": "are3@mail.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
