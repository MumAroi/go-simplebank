info:
  name: create account
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:3010/accounts
  body:
    type: json
    data: |
      {
        "owner": "Tom",
        "currency": "USD"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
