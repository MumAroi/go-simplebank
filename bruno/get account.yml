info:
  name: get account
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:3010/accounts/:id
  params:
    - name: id
      value: "1681"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
